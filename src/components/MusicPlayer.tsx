import React from "react";
import styled from "styled-components";

const MusicPlayerWrapper = styled.div`
  width: 120px;
  height: 50px;
  background-color: red;
`;

const MusicPlayer = () => <MusicPlayerWrapper></MusicPlayerWrapper>;

export default MusicPlayer;
